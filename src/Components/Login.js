import React from "react";
import { Link } from "react-router-dom";

const Login = () => {
  return (
    <React.Fragment>
      <Link to="/main">Login</Link>
    </React.Fragment>
  );
};

export default Login;
